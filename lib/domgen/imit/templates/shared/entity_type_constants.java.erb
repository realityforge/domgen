/* DO NOT EDIT: File is auto-generated */
package <%= to_package(repository.imit.qualified_entity_type_constants_name) %>;

@java.lang.SuppressWarnings( { "UnusedDeclaration", "JavaDoc" } )
public final class <%= repository.imit.entity_type_constants_name %>
{
<%
repository.data_modules.select { |data_module| data_module.imit? }.each do |data_module|
  data_module.entities.each do |entity|
    if entity.imit? && entity.concrete? -%>
  /**
   * The type id of the "<%= entity.name %>" entity.
   */
  public static final int <%= Reality::Naming.uppercase_constantize(entity.data_module.name.to_s) %>_<%= Reality::Naming.uppercase_constantize(entity.name.to_s) %> = <%= entity.imit.transport_id %>;

  /**
   * The name of the "<%= entity.name %>" entity.
   * To ensure these constants are removed in production code, the constants should only be used when
   * guarded by <code>replicant.Replicant.areNamesEnabled()</code>.
   */
  public static final String <%= Reality::Naming.uppercase_constantize(entity.data_module.name.to_s) %>_<%= Reality::Naming.uppercase_constantize(entity.name.to_s) %>_NAME = "<%= entity.qualified_name %>";

<%
    end
  end
end
%>

  private <%= repository.imit.entity_type_constants_name %>()
  {
  }
}
