/* DO NOT EDIT: File is auto-generated */
package <%= to_package(repository.imit.qualified_system_metadata_name) %>;

final class <%= repository.imit.system_metadata_name %>
{
<% repository.imit.graphs.each do |graph| -%>
  @javax.annotation.Nonnull
  private static final replicant.server.transport.ChannelMetaData <%= Reality::Naming.uppercase_constantize(graph.name) %> = new replicant.server.transport.ChannelMetaData( <%= repository.imit.qualified_subscription_constants_name %>.<%= Reality::Naming.uppercase_constantize(graph.name) %>, "<%= graph.name %>", <% if graph.instance_root? %><%= repository.imit.qualified_entity_type_constants_name %>.<%= Reality::Naming.uppercase_constantize(graph.instance_root.to_s.gsub('.','_')) %><% else %>null<% end %>, replicant.server.transport.ChannelMetaData.FilterType.<%= !graph.filtered? ? 'NONE' : graph.filter_parameter.nil? ? 'INTERNAL' : graph.filter_parameter.immutable? ? 'STATIC' : 'DYNAMIC' %>, <%= graph.filter_parameter.nil? ? 'null' : "#{graph.filter_parameter.referenced_struct.action.qualified_json_encoder_name}::decode" %>, replicant.server.transport.ChannelMetaData.CacheType.<%= graph.cacheable? ? 'INTERNAL' : 'NONE' %>, <%= graph.external_visibility? %><%= graph.required_type_graphs.empty? ? '' : ', ' %><%= graph.required_type_graphs.collect{|g|Reality::Naming.uppercase_constantize(g.name)}.join(', ') %> );
<% end -%>
  @javax.annotation.Nonnull
  private static final replicant.server.transport.SchemaMetaData $SYSTEM_META_DATA$ = new replicant.server.transport.SchemaMetaData( "<%= repository.name %>", <%= repository.imit.graphs_code_spaced.collect{|graph| graph.nil? ? 'null' : Reality::Naming.uppercase_constantize(graph.name) }.join(', ') %> );

  private <%= repository.imit.system_metadata_name %>()
  {
  }

  @javax.annotation.Nonnull
  static replicant.server.transport.SchemaMetaData getSchemaMetaData()
  {
    return $SYSTEM_META_DATA$;
  }
}
