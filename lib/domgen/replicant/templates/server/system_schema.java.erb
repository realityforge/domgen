/* DO NOT EDIT: File is auto-generated */
package <%= to_package(repository.replicant.qualified_system_schema_name) %>;

final class <%= repository.replicant.system_schema_name %>
{
<% repository.replicant.datasets.each do |dataset| -%>
  @javax.annotation.Nonnull
  private static final replicant.server.transport.Dataset <%= Domgen::Naming.uppercase_constantize(dataset.name) %> = new replicant.server.transport.Dataset( <%= repository.replicant.qualified_dataset_constants_name %>.<%= Domgen::Naming.uppercase_constantize(dataset.name) %>, "<%= dataset.name %>", <% if dataset.instance_dataset? %><%= repository.replicant.qualified_entity_type_constants_name %>.<%= Domgen::Naming.uppercase_constantize(dataset.dataset_root_entity_type.to_s.gsub('.','_')) %><% else %>null<% end %>, replicant.server.transport.Dataset.FilterMode.<%= dataset.filter_mode.to_s.upcase %>, <%= dataset.parameter_filtered? ? "replicant.server.transport.Dataset.FilterParameterMode.#{dataset.filter_parameter.mode.to_s.upcase}" : 'null' %>, <%= dataset.keyed? %>, replicant.server.transport.Dataset.CacheType.<%= dataset.cacheable? ? 'INTERNAL' : 'NONE' %>, <%= dataset.external_visibility? %><%= dataset.required_type_datasets.empty? ? '' : ', ' %><%= dataset.required_type_datasets.collect{|dataset|Domgen::Naming.uppercase_constantize(dataset.name)}.join(', ') %> );
<% end -%>
  @javax.annotation.Nonnull
  private static final replicant.server.transport.SystemSchema $SYSTEM_SCHEMA$ = new replicant.server.transport.SystemSchema( "<%= repository.name %>", <%= repository.replicant.datasets_code_spaced.collect{|dataset| dataset.nil? ? 'null' : Domgen::Naming.uppercase_constantize(dataset.name) }.join(', ') %> );

  private <%= repository.replicant.system_schema_name %>()
  {
  }

  @javax.annotation.Nonnull
  static replicant.server.transport.SystemSchema getSystemSchema()
  {
    return $SYSTEM_SCHEMA$;
  }
}
