/* DO NOT EDIT: File is auto-generated */
package <%= object_type.data_module.java.package %>.dao;

@SuppressWarnings( { "UnusedDeclaration" } )
@javax.annotation.Generated( "Domgen" )
public final class <%= object_type.java.qualified_name %>DAO
{
<% object_type.jpa.queries.each do |query| %>
  public static <%= query.singular? ? '' : 'java.util.List<' %><%= object_type.java.qualified_name %><%= query.singular? ? '' : '>' %> <%= query.local_name %>(<%= query.parameters.collect {|p| "final #{query.parameter_types[p]} #{p}"}.join(', ') %>)
  {
    final javax.persistence.TypedQuery<<%= object_type.java.qualified_name %>> query =
      SchemaEntityManager.get().createNamedQuery( <%= query.qualified_name %>, <%= object_type.java.qualified_name %>.class );
<% query.parameters.each do |p| %>
    query.setParameter("<%= p %>", <%= p %>);
<% end %>
    return query.<%= query.singular? ? 'getSingleResult' : 'getResultList' %>();
  }
<% end %>

  /** Remove the given  <%= object_type.java.qualified_name %> from the persistence context, causing a managed entity to become detached. */
  public static void detach( final <%= object_type.java.qualified_name %> entity )
  {
    SchemaEntityManager.get().detach( entity );
  }

  /** Merge the state of the given <%= object_type.java.qualified_name %> into the current persistence context. */
  public static <%= object_type.java.qualified_name %> merge( final <%= object_type.java.qualified_name %> entity )
  {
    return SchemaEntityManager.get().merge( entity );
  }

  /** Make an <%= object_type.java.qualified_name %> instance managed and persistent. */
  public static void persist( final <%= object_type.java.qualified_name %> entity )
  {
    SchemaEntityManager.get().persist( entity );
  }

  /** Save a <%= object_type.java.qualified_name %> instance, merging it into the persistent context if required. */
  public <%= object_type.java.qualified_name %> save( final <%= object_type.java.qualified_name %> entity )
  {
    final <%= object_type.java.qualified_name %> e = merge( entity );
    SchemaEntityManager.get().persist( e );
    return e;
  }

  /** Refresh the state of the <%= object_type.java.qualified_name %> instance from the database, overwriting changes made to the entity, if any. */
  public static void refresh( final <%= object_type.java.qualified_name %> entity )
  {
    SchemaEntityManager.get().refresh( entity );
  }

  /** Remove the <%= object_type.java.qualified_name %> instance. */
  public static void remove( final <%= object_type.java.qualified_name %> entity )
  {
    SchemaEntityManager.get().remove( entity );
  }
}